Coloque aqui:
- unario-java.png: print do terminal do servidor e do cliente Java rodando ConsultarHorario, com a saída do comando `Get-Date` visível em algum terminal do print.
- unario-python.png: mesma evidência, para a versão Python.

Estes prints precisam ser capturados manualmente na sua máquina (execução real), não podem ser gerados automaticamente.
